<% header = link_to "All Posts", posts_path%>
<%= render "shared/title",title: "#{header} &raquo; #{@category.name}".html_safe %>

<% @category.posts.each do |post|%>
	<%= render "posts/show_posts", post: post%>
<% end%>